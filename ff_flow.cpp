#include <iostream>
#include <iomanip>
#include <vector>

using namspace std;

int main(){
  int rows,cols;
  cin>>rows,cols;
  vector<vector <int> > graph(rows, vector<int>(cols) );
  int source, sink;

  for(int i=0; i<rows ; i++){
    for(int j=0; j<cols; ++j){
      
    }
  }

  return(0);
}
